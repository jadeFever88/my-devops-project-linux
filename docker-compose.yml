services:
  web-site:
    image: nginx:alpine
    restart: always
    ports:
      - "9091:80"
    volumes:
      - .:/usr/share/nginx/html

  stats:
    image: htop-image # Це просто для прикладу, пізніше поставимо справжню БД
